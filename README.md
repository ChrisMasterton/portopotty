# Port-o-Potty

Tauri (Rust) + React app that monitors port ranges and shows which processes are listening, with one-click kill.

## Dev

Prereqs: Rust toolchain + Node.js.

```bash
npm install
npm run tauri dev
```

## Build

```bash
npm run tauri build
```

